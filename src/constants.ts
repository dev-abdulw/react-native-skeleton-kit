export const SkeletonColors = {
  base: '#E1E9EE',
} as const;

export const SkeletonDefaults = {
  width: '100%' as const,
  height: 16,
  borderRadius: 4,
  duration: 800,
  minOpacity: 0.4,
  maxOpacity: 1,
} as const;
